# Requirements
1. createIssue(tranId, issueType, subject, desc, email)
2. addAgent(email, name, issueTypes)
3. assignIssue(issueId) - with diff strategy
4. getIssue(filter)
5. updateIssue(id, status, resolution)
6. resolveIssue(id, resolution)
7. viewAgentsWorkflow()

issueTypes -> payment, insurance, mutual fund, gold 